version: '3.8'

services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_ops_hub_site
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/v1
